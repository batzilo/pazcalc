const int m = 6;
const int n = 5;
const int q = 7;

PROGRAM foo() {
    int a[m][n]; //, b[n][q]; //, c[m][q];
    int i,j,k;

    FOR (i, 0 TO m-1) {
        FOR (j, 0 TO n-1) {
            a[i][j] = i+j;
            //WRITESP(a[i][j]);
        }
        WRITELN();
    }

    return;
}

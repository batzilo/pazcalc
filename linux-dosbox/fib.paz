FUNC int fib(int n) {
    WRITELN(n);
    if ( n == 1 ) return 1;
    if ( n == 2 ) return 1;
    /*
    int y;
    y = fib(x-1);
    WRITELN(y);
    int z;
    z = fib(x-2);
    WRITELN(z);
    return y+z;
    */
    return fib(n-1) + fib(n-2);
}

PROGRAM bar() {
    int x = fib(3);
    WRITELN(x);
    return;
}

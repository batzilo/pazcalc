FUNC int fact(int x) {
    if (x <= 0) return -1;
    if (x == 1) return 1;
    return x * fact(x-1);
}

PROGRAM bar ( ) {
    int x;
    x = fact(-42);
    WRITELN(x);
    x = fact(7);
    WRITELN(x);
    return;
}
